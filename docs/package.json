{
  "name": "tns-mirror-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Documentation site for tns-mirror, built with Eleventy and Liquid.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf _site",
    "// build": "Cleans first: Eleventy leaves stale output behind, so a renamed or deleted page would otherwise still be deployed. Pagefind indexes the built HTML, so it runs after.",
    "build": "npm run clean && eleventy && pagefind --site _site",
    "// dev": "Builds and indexes once so search works, then serves with live reload.",
    "dev": "npm run build && eleventy --serve --incremental"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "markdown-it-anchor": "^9.2.0",
    "pagefind": "^1.4.0"
  }
}
